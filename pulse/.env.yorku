# Loaded only for Vite mode "yorku" (npm run dev:yorku / build:yorku).
# Selects the York U Fitness classroom branding. The default build does NOT
# load this file, so it stays standard Pulse.
VITE_BRAND=yorku
